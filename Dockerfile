# Використовуємо офіційний образ Node.js
FROM node:latest

# Створюємо робочу директорію в контейнері
WORKDIR /app

# Копіюємо файли додатка в робочу директорію
COPY . .

# Встановлюємо залежності через npm
RUN npm install

# Вказуємо команду, яка буде запускатися при старті контейнера
CMD ["node", "app.js"]
